# GitHub App manifest (permissions + events) used to register the app.
name: promptblock
description: Scans GitHub issues and comments for prompt injection, including content hidden in HTML comments.
public: false

default_permissions:
  # Read issue/comment bodies and write labels + warning comments.
  issues: write
  metadata: read

default_events:
  - issues
  - issue_comment
